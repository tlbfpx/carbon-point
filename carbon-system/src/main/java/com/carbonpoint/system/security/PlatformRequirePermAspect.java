package com.carbonpoint.system.security;

import org.springframework.stereotype.Component;

/**
 * Temporarily disabled - AspectJ pointcut expression causing startup errors.
 * Re-enable once the inconsistent binding issue is resolved.
 */
// @Aspect
// @Component
public class PlatformRequirePermAspect {
    // Disabled due to AspectJ pointcut parsing error with Java 21
}
